fБинарная куча — это структура данных, представляющая собой полное бинарное дерево, которое удовлетворяет свойству кучи.
Полное бинарное дерево все уровни полностью заполнены, кроме, возможно, последнего, который заполняется слева направо
Свойство кучи Min-Heap каждый узел ≤ своих потомков (корень - минимальный элемент); Max-Heap каждый узел ≥ своих потомков (корень - максимальный элемент)

Биномиальная куча — это набор биномиальных деревьев, обладающий свойствами min-heap или max-heap. 
Состоит из коллекции биномиальных деревьев
Каждое биномиальное дерево степени k имеет
Ровно 2ᵏ узлов
Высоту k
Ровно C(k, d) узлов на глубине d

Куча Фибоначчи — это коллекция деревьев, подчиняющихся свойству min-heap, с оптимизированными операциями уменьшения ключа и объединения.
Расслабленная структура деревья могут иметь любую форму
Отложенная консолидация объединение деревьев откладывается
Маркировка узлов отслеживание потери потомков

Хеш-таблица — это структура данных, реализующая интерфейс ассоциативного массива с использованием хеш-функции для вычисления индекса.

Языки реализации
Python (heapq, dict + простые классы)
Java (PriorityQueue, HashMap + классы-заготовки)
C++ (priority_queue, unordered_map + классы-заготовки)

Пример реализации
import heapq

# Встроенная реализация
heap = []
heapq.heappush(heap, 5)
heapq.heappush(heap, 2)
min_element = heapq.heappop(heap)  
